// File Organization

Put node file in main folder, then put Html, css, and javascript in a seperate 
folder within the main folder with the node with the name PUBLIC

// Navigate to Project Directory

// Navgate to the project directory

npm init -y

// Install Express

npm install express

// Run Server

node Node.js (Node file name)
